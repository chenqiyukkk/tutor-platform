DOMAIN="example.com"
TLS_EMAIL="ops@example.com"
APP_URL="https://example.com"

POSTGRES_DB="tutor_platform"
POSTGRES_USER="tutor"
POSTGRES_PASSWORD=""
# Use the same URL-safe password as POSTGRES_PASSWORD. The host must stay `db`.
DATABASE_URL=""

# Generate at least 32 random characters. Never reuse the database password.
SESSION_SECRET=""

# Required before production password recovery is enabled.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""
